# Non-ROS Python dependencies for the cortex cognitive plane.
# ROS deps (rclpy, std_msgs, g1_onboard_msgs, ...) come from the Humble apt
# install / colcon workspace, not here.
#
# Versions mirror kist-drl-g1-workstation's pyproject.toml so the ported STT/TTS
# paths behave identically.

# --- scenarios (cortex_cognition) ---
json5
pyyaml

# --- audio DSP + resample (perception stt filter, action tts resample) ---
numpy==1.26.4
scipy>=1.15.3

# --- STT: Google Cloud Speech-to-Text v1 bidi streaming [REQ-27] ---
google-cloud-speech>=2.21.0

# --- TTS: Naver CLOVA Voice Premium REST [REQ-29] ---
aiohttp==3.13.4

# --- LLM subtask planning (cortex_cognition llm_node) [SYS-REQ-44] ---
# Either SDK is optional at runtime (backend: dummy needs neither).
google-genai>=1.0
openai>=1.0

# --- detector_node (cortex_perception) — optional; without it CheckTarget answers model_not_loaded
# ultralytics>=8.3      # pulls torch + opencv; install on the workstation only
# --- gui_bridge_node ---
websockets>=12
